const plateformController = require('./plateform');
const purchaseController = require('./purchase');
const userController = require('./user');
const gameController = require('./game');

module.exports = {
  plateformController,
  purchaseController,
  userController,
  gameController,
};
